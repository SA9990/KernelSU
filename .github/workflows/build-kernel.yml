name: Kernel Build

on:
  push:
    branches:
      - main      # Adjust this to your desired branch.
  pull_request:
    branches:
      - main      # Trigger on pull requests to the main branch.
  workflow_dispatch:  # Allow manual triggering of the workflow.

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the current repository
      - name: Checkout Repository
        uses: actions/checkout@v3
      
      # Step 2: Install required packages
      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y git wget curl unzip python3 zip

      # Step 3: Clone Kernel Source from Bliss ROMs repository
      - name: Clone Kernel Source
        run: |
          git clone --depth=1 https://github.com/BlissRoms-Devices/android_kernel_asus_sm8250.git kernel
          cd kernel

      # Step 4: Download and Setup Custom Clang toolchain
      - name: Download and Setup Custom Clang
        run: |
          git clone https://github.com/kdrag0n/proton-clang.git --depth=1 clang
          export PATH=$(pwd)/clang/bin:$PATH

      # Step 5: Set up environment variables
      - name: Set Up Environment Variables
        run: |
          echo "ARCH=arm64" >> $GITHUB_ENV
          echo "SUBARCH=arm64" >> $GITHUB_ENV
          echo "CROSS_COMPILE=aarch64-linux-gnu-" >> $GITHUB_ENV
          echo "CROSS_COMPILE_ARM32=arm-linux-gnueabi-" >> $GITHUB_ENV
          echo "CLANG_TRIPLE=aarch64-linux-gnu-" >> $GITHUB_ENV
          echo "PATH=$(pwd)/clang/bin:$PATH" >> $GITHUB_ENV

      # Step 6: Configure the kernel (use device-specific defconfig)
      - name: Configure Kernel
        run: |
          cd kernel  # Change to the kernel directory
          make O=out vendor/obiwan_defconfig   # Use the correct defconfig for your device
          
      # Step 7: Compile the kernel with Clang
      - name: Compile Kernel
        run: |
          cd kernel  # Ensure you are in the kernel directory
          make -j$(nproc) O=out ARCH=arm64 CC=clang LLVM=1

      # Step 8: Upload the compiled kernel artifacts (optional)
      - name: Upload Compiled Kernel Image
        uses: actions/upload-artifact@v3
        with:
          name: kernel-image
          path: kernel/out/arch/arm64/boot/Image.gz  # Or the appropriate path for the kernel image
